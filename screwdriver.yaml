shared:
    image: node:6

jobs:
    job1:
        steps:
          - mystep: echo hi
          - fail: exit
          - teardown-step1: echo hello
          - anotherstep: echo bye
          - teardown-step2: echo world
        requires: [~commit, ~pr]
